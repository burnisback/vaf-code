/**
 * Architecture Components
 *
 * React components for displaying and interacting with architecture documents.
 *
 * @example
 * ```tsx
 * import { ArchitectureViewer } from '@/components/bolt/architecture';
 *
 * <ArchitectureViewer
 *   architecture={arch}
 *   onStartImplementation={(phaseId) => console.log('Start', phaseId)}
 * />
 * ```
 */

export { ArchitectureViewer } from './ArchitectureViewer';
