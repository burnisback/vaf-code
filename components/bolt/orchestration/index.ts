/**
 * Orchestration Components
 *
 * React components for monitoring and controlling multi-phase orchestration.
 *
 * @example
 * ```tsx
 * import { OrchestrationPanel, OrchestrationStatus } from '@/components/bolt/orchestration';
 *
 * <OrchestrationPanel
 *   state={state}
 *   context={context}
 *   progress={progress}
 *   onApprove={handleApprove}
 *   onPause={handlePause}
 * />
 * ```
 */

export { OrchestrationPanel, OrchestrationStatus } from './OrchestrationPanel';
