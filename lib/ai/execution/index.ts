/**
 * Execution Module Index
 *
 * Central export for parallel execution components.
 */

export {
  ParallelExecutor,
  parallelExecutor,
  executeParallel,
  runParallel,
  parallelMap,
  type ParallelTask,
  type TaskResult,
  type ParallelExecutionResult,
  type ParallelExecutorConfig,
} from './parallel';
